import React from 'react'
import { Itemcard } from './Itemcard'

export const BattleNamesList = (data, name) => {
    return (
        <div>
            
             {/* {data.map((item) => {
            return(
                <Itemcard name={item}/>
            )
        })} */}
        </div>
    )
}
